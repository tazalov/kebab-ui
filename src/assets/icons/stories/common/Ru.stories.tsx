import type { Meta } from '@storybook/react'

import { RuFlag } from '../../'

export default {
  component: RuFlag,
  title: 'icons/common/RuFlag',
} satisfies Meta<typeof RuFlag>

export const Default = {}
