import type { Meta } from '@storybook/react'

import { Phone } from '../../'

export default {
  component: Phone,
  title: 'icons/common/Phone',
} satisfies Meta<typeof Phone>

export const Default = {}
