import type { Meta } from '@storybook/react'

import { Logout } from '../../'

export default {
  component: Logout,
  title: 'icons/common/Logout',
} satisfies Meta<typeof Logout>

export const Default = {}
