import type { Meta } from '@storybook/react'

import { Menu } from '../../'

export default {
  component: Menu,
  title: 'icons/common/Menu',
} satisfies Meta<typeof Menu>

export const Default = {}
