import type { Meta } from '@storybook/react'

import { Expand } from '../../'

export default {
  component: Expand,
  title: 'icons/common/Expand',
} satisfies Meta<typeof Expand>

export const Default = {}
