import type { Meta } from '@storybook/react'

import { Check } from '../../'

export default {
  component: Check,
  title: 'icons/common/Check',
} satisfies Meta<typeof Check>

export const Default = {}
