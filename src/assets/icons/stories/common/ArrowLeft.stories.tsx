import type { Meta } from '@storybook/react'

import { ArrowLeft } from '../../'

export default {
  component: ArrowLeft,
  title: 'icons/common/ArrowLeft',
} satisfies Meta<typeof ArrowLeft>

export const Default = {}
