import type { Meta } from '@storybook/react'

import { Close } from '../../'
export default {
  component: Close,
  title: 'icons/common/Close',
} satisfies Meta<typeof Close>

export const Default = {}
