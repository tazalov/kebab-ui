import type { Meta } from '@storybook/react'

import { Search } from '../../'

export default {
  component: Search,
  title: 'icons/common/Search',
} satisfies Meta<typeof Search>

export const Default = {}
